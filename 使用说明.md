# SCIDownload 使用步骤

> 发布版：**v1.6.1**　／　目录共 10 个文件，见文末「文件一览」

只自动完成你**本来就有权进行**的浏览器下载。不提供订阅，也不绕过付费访问控制。

---

## 前提

1. **Python 3.10+**
2. **Chrome / Edge / Chromium**
3. **在校园网内，或连着学校 VPN**（机构订阅认出口 IP，不认账号）

> ⚠️ 机构若靠校园出口 IP 授权，**别让出版商域名走第三方代理**。
> 若学校用 CARSI / EZproxy / VPN 授权，按学校给的方式访问。
> `1.check_exit_ip.py` 只排查网络路径，**不能证明你有没有订阅**。

> 📁 `3.SCIDownload.py` 与 `SCIDownload_cdp.py` **必须同目录**，别拆开。
> **无需 `pip install`** —— 全部只用标准库。

---

## 先确认 `python` 命令能用

说明里统一写 `python`，因为它在你电脑上对应哪个位置得你自己确认。

**第 1 步：试一下**

```powershell
python --version
```

| 结果 | 怎么办 |
|---|---|
| `Python 3.10.x` 以上 | ✅ 直接用，不用改 |
| 弹出微软商店 / `Python was not found` | ❌ 这是 Windows 自带的**假 python** |
| 不是内部或外部命令 | ❌ 不在 PATH 里 |
| 3.9 或更低 | ❌ 版本太老，装 3.10+ |

**第 2 步：找到你的真实路径**（三选一）

- **最省事**：开始菜单搜 `Python 3.x` → 右键快捷方式 →「打开文件位置」→ 再右键 →「属性」→ 看「目标」。长这样（**你的不同**）：
  `C:\Users\你的用户名\AppData\Local\Programs\Python\Python312\python.exe`
- **conda / Miniforge**：一般在安装目录根下，如 `D:\miniforge3\python.exe`
- **直接问系统**：`where.exe python`，从上往下第一个通常就是能用的

**第 3 步：怎么用**（二选一）

**① 把路径写进命令**（最直接，**双引号不能省**）

```powershell
"C:\Users\你的用户名\AppData\Local\Programs\Python\Python312\python.exe" 3.SCIDownload.py --limit 3 --si
```

以后看到说明里以 `python` 开头的命令，都把 `python` 换成你那一串带引号的完整路径即可。

**② 永久加进 PATH**（之后就能直接敲 `python`）—— 路径到目录为止，**不带 `python.exe`**：

```powershell
setx PATH "$env:PATH;C:\Users\你的用户名\AppData\Local\Programs\Python\Python312"
```

然后**关掉终端重开**，再敲 `python --version` 验证。

---

## 1. 打开终端并进入目录

**用 VS Code（推荐）**：「文件」→「打开文件夹」→ 选中解压出来的目录 →
按 `Ctrl` + 反引号（`Esc` 下面那个键）打开集成终端 —— **自动落在该目录，`cd` 都省了**。

⚠️ **别点右上角的 ▷ 运行按钮** —— 它不带参数运行，等于没给清单。必须在**集成终端**手打命令。

**用 cmd / PowerShell**：

```powershell
cd /d "你解压的完整路径"
```

验证环境就位：

```powershell
python 3.SCIDownload.py --help
```

能打印参数表就对了。若报「can't open file」，说明你**不在发布包目录里**。

---

## 2. 准备 DOI 清单

### 做法 A：自己搜集 DOI（推荐，最准）

| 来源 | 说明 |
|---|---|
| Web of Science / Scopus | 学校图书馆多有；检索结果直接有 DOI 列 |
| Google Scholar | 点进条目，页面上写着 DOI |
| 出版商文章页 | 地址栏 `doi.org/10.xxxx/...` 后面那段 |
| 参考文献列表 | 很多期刊直接标 DOI |

**存成 `dois.txt`，每行一个**：

```text
10.1016/j.fcr.2011.11.020
10.1016/j.agee.2018.04.017
10.1002/ghg.1892
```

⚠️ **不要加序号、逗号、标题**。前后有空格没关系，程序会清理。

**也支持表格**：`.csv` / `.tsv` / `.xlsx` / `.xlsm`，用 `DOI` 当表头（可选加 `Num`、`Title`；
`Num` 会出现在文件名开头，方便对号）。旧版 `.xls` 请另存为 `.xlsx` 或 `.csv`。

### 做法 B：只有标题，让程序反查

```powershell
python 2.FindDOI.py 我的清单.xlsx
```

生成 `我的清单_带DOI.csv`，**可直接拿去下载**。

| 参数 | 说明 |
|---|---|
| `--out 结果.csv` | 指定输出名（默认 `<输入名>_DOI.csv`） |
| `--mailto 你的邮箱` | **可省略**；填了会随请求发给 OpenAlex / Crossref，更快更稳 |
| `--limit 5` | 只处理前 5 条，先试效果 |
| `--force` | 忽略缓存，全部重查 |

**⚠️ 必读**：标题检索一定返回一堆「相关但不相同」的论文，**填错就是下错文献**。
所以程序**只给高置信度的行填 DOI**：

| 状态 | 含义 | 你该做什么 |
|---|---|---|
| `已确认` | 相似度 ≥ 0.97 | 不用管 |
| `待确认` | 0.80～0.97 | **看窗口里标出的差异** |
| `未找到` | < 0.80 | 自己去找 |

「待确认」会在末尾列出，**多出来的词用 `[]` 标出**：

```text
输入：The effects of Chinese milk vetch returning with nitrogen fertilizer on rice
命中：...on rice [yield and greenhouse gas emissions]
```

中括号里就是差异 —— 本例是同一篇（标题写短了）；**差异很大就不是同一篇**。
这类行的 DOI 列**是空的**，需要你核对后手工填。

> 💡 查 DOI 走**公网**（OpenAlex + Crossref），**开着代理反而更稳**。
> 与第 4 步正好相反 —— 下载必须走校园网 IP。

> ❌ **不支持中文文献**：CNKI / 万方不在 OpenAlex / Crossref 索引内。

### 文件名可以不写

**当前目录只有一个**受支持清单时，可省略文件名：

```powershell
python 3.SCIDownload.py --limit 3 --si
```

程序会打印「自动发现唯一候选：xxx」。规则：**1 个**→自动用；**0 个**→提示放入清单；
**2 个以上**→列出并退出，**不替你猜**（防下错）。`requirements.txt` 这类环境文件不会被当成清单。

> 💡 建议**始终显式写文件名**，更不容易错，也方便复用命令。

---

## 3. 检查网络（可选，建议第一次跑之前做）

```powershell
python 1.check_exit_ip.py
```

显示：代理环境变量（**账号密码已隐藏**）、本机常见代理端口、出口 IP（**默认打码**）。

**它只是排障工具。** 代理、浏览器分流、学校 VPN 都可能让探测结果与实际文章会话不同 ——
**最终以文章页授权状态和真实下载结果为准。**

> 想看完整 IP 加 `--show-full-ip`。**要发截图时千万别加。**

---

## 4. 先试跑 3 篇

```powershell
python 3.SCIDownload.py --limit 3 --si
```

（有多个候选清单时改成 `python 3.SCIDownload.py dois.txt --limit 3 --si`）

然后打开 `SCIDownload-out\PDFs\` 看文件能否正常打开。

### ⚠️ 试跑可能遇到的情况

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 某篇 `no_pdf_link`（页面可访问但没解析到入口） | **多半是第一次跑、会话没预热好** | **原样再跑一次**，通常就好 |
| `needs_manual_login` | 机构登录态过期 | 按第 5 节加 `--login-wait 300` |
| `no_entitlement` | 机构确实没订这本刊 | 不是程序问题，换途径 |

> 📌 实测结论：同一个 DOI，第一次 `no_pdf_link`、原样重跑就成功。**先重跑，别急着改代码。**

**一篇约 10～12 秒，100 篇约 30 分钟。** 实际随网络和出版商变化；
默认每 20 篇休息约 2 分钟，用来降低被限流风险。

**这个阶段不要做**：❌ 开多个窗口同时跑 ❌ 调小 `--pace` / `--batch-size` ❌ 猛冲失败篇目。

### 🔴 跑完记得检查浏览器默认下载目录（重要）

**部分出版商（实测含 MDPI，以及部分 CNKI / 中文期刊）的 PDF 走的是浏览器「另存为」式下载，
可能没被程序接住，而是**直接落到浏览器的默认下载目录**。这时程序会报：

```text
PDF ✗ browser download was signaled but no file appeared
```

**这句不是说没下载，而是说下载了但没落到程序目录里。**

**所以每批跑完后，请手动检查你的下载目录：**

| 系统 | 默认位置 |
|---|---|
| Windows | `C:\Users\<你的用户名>\Downloads` |
| macOS | `~/Downloads` |

- 先在浏览器里看 **`chrome://downloads`**（Edge 用 `edge://downloads`），能直接看到这批下到了什么、存在哪
- 找到的 PDF **手工移进** `SCIDownload-out\PDFs\`
- 注意别留重复：如果程序重试过，那里可能出现 `xxx.pdf`、`xxx (1).pdf`、`xxx (2).pdf` —— **按大小和日期留最新的一个，其余的删掉**

> 💡 想从源头避免，可以在**这个专用浏览器窗口**里把下载目录也指到 `SCIDownload-out\PDFs\`。
> 但最稳的还是**跑完手动核对一遍**。

---

## 5. 参数怎么加、怎么删

| 参数 | 加上它 | **不需要时** |
|---|---|---|
| `--si` | 同时下载补充材料，存到 `SupportingInformation\` | **整段删掉**，只下正文。`--si` 与 `--no-si` 互斥 |
| `--limit 3` | 只跑前 3 篇，**试跑专用** | **全量时整段删掉**，否则只下前 3 篇 |
| `--only "doi1,doi2"` | 只跑指定几篇，不区分大小写 | 删 |
| `--out ./papers` | 换输出目录（默认 `./SCIDownload-out`） | 用默认值就删 |
| `--login-wait 300` | 停在机构登录页时等你登录，最多 5 分钟 | 登录态已保存后就不需要 |

**三段对照**：

```powershell
# 试跑（要 SI，只 3 篇）
python 3.SCIDownload.py dois.txt --limit 3 --si

# 全量（要 SI）：把 --limit 3 删掉
python 3.SCIDownload.py dois.txt --si

# 只要正文：把 --si 删掉（或写 --no-si）
python 3.SCIDownload.py dois.txt
```

**首次机构登录**（走 CARSI / EZproxy / 账号的机构）：

```powershell
python 3.SCIDownload.py dois.txt --limit 3 --login-wait 300
```

会**弹出浏览器窗口**，你在里面登录。专用 profile **保存登录态**，以后不用再登。

### 其他参数

| 参数 | 用途 |
|---|---|
| `--force` | 忽略断点，**全部重做**。确认要重下才用 |
| `--port 9222` | CDP 端口。**提示被占用就换一个**，如 `--port 9223` |
| `--profile <目录>` | 指定独立浏览器 profile |
| `--browser "<路径>"` | 浏览器没被自动找到时手动指定 |
| `--headless` | 无头模式（**不推荐**，部分站点兼容性差） |

⚠️ `--pace` / `--batch-size` / `--batch-rest` 是**节奏控制，不是速度旋钮**，**建议保持默认**。
⚠️ `--keep-browser` 结束后不关浏览器，**只适合你自己的电脑**。

完整列表：`python 3.SCIDownload.py --help`

---

## 6. 暂停、恢复与输出

**暂停**：`Ctrl+C`，然后**原样重跑**即可恢复，已下好的自动跳过。

**不要**：❌ `Ctrl+S`（只冻结显示，程序还在跑） ❌ 任务管理器「挂起」（会话会僵死超时）

```text
SCIDownload-out/
├── PDFs/                      ← 正文
├── SupportingInformation/     ← 补充材料（用了 --si 才有）
├── manifest.jsonl             ← 每篇一行的状态记录
└── run.log                    ← 运行日志
```

**续跑判断**：程序**不只信记录，还验证磁盘真实文件** —— 正文和 SI 都在且有效→跳过；
正文在、SI 缺→**只补 SI**；文件缺失/损坏→回到补下载流程。所以一般不需要 `--force`。

**命名**：`151_10.1016_j.fcr.2011.11.020.pdf`（有 `Num`）或 `10.1016_j.fcr...pdf`（无）。
DOI 里的 `/`→`_`、`:`→`-`。**题名不参与命名。**

**完整性**：先写临时文件、验证长度和文件头、再原子替换。失败**不会覆盖**已下好的正文。

---

## 7. 清理浏览器缓存

```powershell
python 4.ClearCache.py --dry-run   # 先看会删什么
python 4.ClearCache.py             # 确认后真清
```

默认**只清缓存，保留 Cookie 和登录态**。

彻底重置（**登录态也清掉，下次要重新登录**）：

```powershell
python 4.ClearCache.py --all
```

> 自定义 `--profile` 的整目录删除还要额外加 `--confirm-custom-profile`。
> 脚本会拒绝磁盘根目录、用户主目录和过浅路径。

---

## 8. 常见状态

| 状态 | 含义 | 怎么办 |
|---|---|---|
| `downloaded` | 正文已验证保存 | 开了 `--si` 再看 `si_status` |
| `no_entitlement` | 当前会话无全文权限 | 查订阅、登录状态、网络路径 |
| `needs_manual_login` | 停在机构登录页 | 登录一次后重跑 |
| `no_pdf_link` | 页面可访问但没解析到入口 | **先原样重跑一次** |
| `unsupported_publisher` | 当前版本没适配 | 自动重试通常无效，换合法途径 |
| `bad_doi` | DOI 打不开文章页 | 核对 DOI 有没有写错 |
| `challenge_not_resolved` | 站点验证长期没结束 | 停，等 30～60 分钟 |
| `error` | 出了异常 | 看 `note` 和 `run.log` |

### 遇到站点验证或限流

| 症状 | 真正原因 | 做什么 |
|---|---|---|
| 连续 `challenge_not_resolved`，浏览器出现验证页或一直空白 | **站点验证 / 限流** | 见下面五条 |
| `no_entitlement`，页面显示 `Get access` | 当前会话没权限 | 查订阅、登录、网络 |

1. **立刻停**（`Ctrl+C`），别硬冲 —— 高频请求可能延长限制
2. **等 30～60 分钟**
3. 恢复时**保持单实例**、放慢节奏
4. **当天别反复冲**
5. **不要开多个窗口同时跑**

已下成的会自动跳过，重跑不重复劳动。

### SI 的两种「没有」

| 状态 | 含义 |
|---|---|
| `none_found` | **已检查过**支持的入口，确实没有独立补充文件。正常结果，不是错误 |
| `not_supported` | 正文能下，但该出版商的 SI 枚举**尚未实现** —— **不等于该文没有 SI** |

（出版商会用 `Data Profile.xml` 这类**占位包**表示「没有独立 SI」，
程序会识别并判为 `none_found`，**不会反复重试**。）

---

## 9. 多出版商

混着别的出版商也能一起跑，**不需要额外参数**。

| DOI 前缀 | 出版商 | 状态 |
|---|---|---|
| `10.1016/*` | Elsevier / ScienceDirect（含 JIA、Pedosphere） | ✅ 主战场 |
| `10.1002` `10.1111` `10.2134` `10.2136` | Wiley | ✅ 走 `/doi/pdfdirect/` |
| `10.1139` `10.4141` | 加拿大科学出版社 | ✅ |
| `10.1071` | CSIRO | ✅ |
| `10.5194` | Copernicus（EGU） | ✅ |
| `10.7717` | PeerJ | ✅ |
| `10.1038` | Nature Portfolio / Scientific Reports | ✅ |
| `10.3390` | MDPI | ⚠️ 走浏览器原生下载，**见第 4 节检查默认下载目录** |
| 其它前缀 | 其它出版商 | ↪️ 先试 `citation_pdf_url` 和页面明确的 PDF 链接 |
| `10.1007` `10.1080` `10.1017` `10.3724` `10.5846` 等 | Springer / T&F / Cambridge / sciengine / 生态学报 | ⚠️ 先走通用回退，失败后归 `unsupported_publisher` |

**⚠️ 最后一行那批不要期待太高**（实测）：

- **生态学报**：返回极小的滑块验证脚本，是反爬拦截页
- **sciengine / 部分 Springer**：返回 HTML 阅读器页，不是 PDF 字节
- **CNKI / 中文期刊**：多数落地在 `chndoi.org` / `cnki.net`，且常触发浏览器原生下载

这些站点**不会只凭 DOI 前缀就提前放弃**，会先解析真实文章页、试通用候选。
都失败才汇总为 `unsupported_publisher`，且**不会在补跑轮反复重试**。

这表示「当前版本没找到可靠的自动下载入口」，
**不代表你的机构一定没订，也不代表手动下载一定不行。**

> 💡 **别当它是「万能下全库」**：有专门规则的站点更稳。
> 拿不准就先 `--limit 3` 试。

---

## 10. 授权边界

浏览器路线**不需要 API key**。Elsevier 官方 API 需要 key；机构订阅有时可自动关联，
校外或特定集成可能需机构 / Elsevier 配置 `insttoken`。
**没有开放获取或订阅权益时，付费全文依然拿不到。**

只下载你被授权、且确有研究需要的材料。遵守出版商条款、机构政策和合理使用限制。
MIT 许可证只覆盖代码，**不授予第三方内容或服务的权利**。

---

## 附：文件一览

**你只需要碰前 4 个 .py**：

| 文件 | 作用 |
|---|---|
| `3.SCIDownload.py` | **主程序** —— 下载文献 |
| `2.FindDOI.py` | 只有标题时反查 DOI |
| `1.check_exit_ip.py` | 检查网络出口（可选） |
| `4.ClearCache.py` | 清理浏览器缓存 |
| `SCIDownload_cdp.py` | 浏览器控制模块。**必须与主程序同目录，别删** |
| `使用说明.md` | 本文件 |
| `README.md` / `SECURITY.md` / `LICENSE` / `.gitignore` | 简介 / 安全说明 / 许可证 / Git 用 |

> 📌 数字前缀只是**阅读顺序**。`2.FindDOI.py` 内部会调用 `3.SCIDownload.py` 的解析函数，
> 所以两者也要放同一目录。

**附：cmd 与 PowerShell 命令不通用**

| 想做的事 | PowerShell（VS Code 默认） | cmd |
|---|---|---|
| 进目录 | `cd "路径"` | `cd /d "路径"` |
| 看 Python 在哪 | `where.exe python` | `where python` |
| 数文件个数 | `(ls SCIDownload-out\PDFs).Count` | `dir /b SCIDownload-out\PDFs \| find /c /v ""` |
